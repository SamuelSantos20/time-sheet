version: '3.8'  # Versão recomendada para novos projetos

services:
  postgres:
    image: 'postgres:17.0'            # Imagem do PostgreSQL na versão 17.0
    container_name: time-sheet-db     # Nome do contêiner (facilita identificação)
    environment:                      # Variáveis de ambiente
      - POSTGRES_DB=time-sheetdb      # Banco de dados inicial
      - POSTGRES_USER=admin           # Usuário do banco
      - POSTGRES_PASSWORD=admin       # Senha do banco
    ports:                            # Mapeamento de portas
      - '5434:5432'                   # Host:5434 → Contêiner:5432 (padrão do PostgreSQL)
    volumes:                          # Persistência de dados
      - postgres_data:/var/lib/postgresql/data
    networks:                         # Rede isolada
      - time-sheet-network

volumes:
  postgres_data:                      # Volume para persistência de dados

networks:
  time-sheet-network:                 # Rede isolada para comunicação entre serviços



#
#🚀 Como usar
#Subir o contêiner:

bash
docker-compose up -d


#Verificar se está rodando:

bash
docker ps


#Acessar o banco de dados:

bash
docker exec -it time-sheet-db psql -U admin -d time-sheetdb




